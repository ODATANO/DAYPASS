# CAP DAYPASS
.env
_out
coverage/
*.db
*.db-shm
*.db-wal
*.sqlite
connection.properties
default-*.json
.cdsrc-private.json
gen/
node_modules/
target/

# Web IDE, App Studio
.che/
.gen/

# MTA
*_mta_build_tmp
*.mtar
mta_archives/

# Other
.DS_Store
*.orig
*.log

*.iml
*.flattened-pom.xml

# IDEs
# .vscode
# .idea

# @cap-js/cds-typer
@cds-models

# Aiken build output (plutus.json IS committed)
contracts/**/build/

# zk/artifacts/ (DAYZERO proving keys + validator registry) IS committed:
# a regenerated setup silently changes the VK and with it the verifier
# policyId, breaking continuity with on-chain attestations.

# Claude Code local settings (personal permissions)
.claude/settings.local.json

# Working files / scratch
Screenshot*.png

# Working notes, bug reports and evidence logs (local only)
IMPLEMENTATION.md
CLAUDE.md
docs/odatano-bugs.md
docs/test-transactions.md
docs/zk-track-decision.md
docs/upstream/
.vscode

# Dev/e2e scripts (local only): live-chain mints, wallet gen, prover pinning.
# The offline unit suite under test/unit/ (+ its fixtures) IS committed and runs
# in CI; other test harnesses (ui-smoke, wallet-sim helpers) stay local.
scripts/
test/*
!test/unit/
!test/fixtures/

# Screenshots regenerated by the local UI smoke run (the walkthrough
# screenshots in docs/screenshots/ ARE committed)
docs/screenshots/cockpit-list.png
docs/screenshots/cockpit-detail.png
docs/screenshots/viewer-consumer.png
docs/screenshots/viewer-recycler.png
docs/screenshots/viewer-authority.png
docs/screenshots/viewer-resolver.png